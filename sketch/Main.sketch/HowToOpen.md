This directory contains a [Sketch](https://www.sketch.com) document saved with the [SketchNoZip](https://www.github.com/iccir/SketchNoZip) plugin.

To open this document:

- Compress this directory into a `.zip` file.
- Change the file extension of the result from `.zip` to `.sketch`.
- Open with Sketch.
